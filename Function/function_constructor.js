let ConstructorFunction = function(value,array) {
    this.value = value;
    this.array = array;

    // if you want to see more details go to object directory
}

let object = new ConstructorFunction("Bro 25",[10,20,30]);
console.log(object);